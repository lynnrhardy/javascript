subordinate(input, subproblem, partial_solution):
	if base_case_condition:
		add partial solution to your results (it will be a fully formed solution)
		return
	
	# decrease subproblem size
	# increment partial solution
	# recursively call subordinates

main(input):
	# initialize subproblem
	# initialize partial solution

	# fill in the blanks
	subordinate(input, subproblem, partial_solution)